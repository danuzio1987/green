<tr>
    <td class="header">
        <a href="{{ $url }}" style="display: inline-block;">
            <h1>⛽ Green Combustíveis ⛽</h1> 
            {{-- 
            @if (trim($slot) === 'Green')
                <img src="{{url('/public/images/logo/icone_green.png')}}" class="logo" alt="Green Distribuidora">
            @else
                {{ $slot }}
            @endif
            --}}
           
        </a>
    </td>
</tr>
